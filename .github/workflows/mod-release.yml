# Concord — reusable release workflow for member mods.
#
# On a v* tag push: builds the mod, writes player-facing release notes, cuts a
# GitHub Release with the mod jar attached (sources/dev jars excluded), and —
# when the matching token is present — publishes the same jar and notes to
# Modrinth (MODRINTH_TOKEN) and CurseForge (CURSEFORGE_TOKEN + curseforge-id)
# with direct REST calls to each platform's upload API. A platform whose token
# or id is absent is skipped, so a member can release to GitHub alone, or to
# GitHub plus either platform. Release notes are generated by Claude from
# GitHub's factual merged-PR list, then published as that prose alone — no
# commit/PR list, contributor roll, or compare links. The raw list becomes the
# body only as a fallback when the model or its token is unavailable.
#
# Game versions and loaders are pinned via inputs (default 1.21.1 / fabric, the
# suite target) rather than read from fabric.mod.json, so a "~1.21.1" metadata
# range can't fan a single release out across every 1.21.x patch.
#
# Optional dependencies are read from fabric.mod.json's suggests/recommends and
# attached to both listings. A mod id whose platform slug differs from its
# Fabric id is remapped through a repo-local .github/release-slug-overrides.json
# (e.g. roughlyenoughitems -> rei / roughly-enough-items). Dependencies are
# best-effort: an unresolvable slug is dropped with a warning, never a failure.
#
# The release is assembled as a draft, the jar is attached, then the draft is
# published — the only order that works on repositories with immutable releases
# enabled, where assets must be uploaded before a release is published.
#
# Mod id defaults to the repository name — the suite convention is
# repo name == mod id == jar prefix. Note generation reuses the suite's
# CLAUDE_CODE_OAUTH_TOKEN (inherited). Caller stub:
#
#   name: Release
#   on:
#     push: { tags: ["v*"] }
#   permissions:
#     contents: write
#   jobs:
#     release:
#       uses: rfizzle/concord/.github/workflows/mod-release.yml@master
#       secrets: inherit
name: Mod Release

on:
  workflow_call:
    inputs:
      mod-id:
        description: "jar prefix / mod id (defaults to the repository name)"
        type: string
        default: ""
      model:
        description: "Claude model used to write the release notes"
        type: string
        default: claude-sonnet-4-6
      changelog-max-budget-usd:
        type: string
        default: "5.00"
      changelog-max-turns:
        type: string
        default: "30"
      modrinth-id:
        description: "Modrinth project slug or id (defaults to the mod id)"
        type: string
        default: ""
      curseforge-id:
        description: "CurseForge numeric project id (blank disables CurseForge)"
        type: string
        default: ""
      game-versions:
        description: "Comma-separated Minecraft versions to tag the release with"
        type: string
        default: "1.21.1"
      loaders:
        description: "Comma-separated mod loaders to tag the release with"
        type: string
        default: "fabric"
    secrets:
      CLAUDE_CODE_OAUTH_TOKEN:
        # Optional: when absent the release body falls back to GitHub's raw
        # auto-generated notes, so a member without the token still releases.
        required: false
      MODRINTH_TOKEN:
        # Optional: a Modrinth PAT with the "Create versions" scope. Absent ->
        # the Modrinth publish step self-skips; the GitHub Release is still cut,
        # so a member without it still releases.
        required: false
      CURSEFORGE_TOKEN:
        # Optional: a CurseForge author upload token (account API token, not the
        # restricted Core/Eternal read key). Needs curseforge-id set to publish;
        # absent or unset -> the CurseForge publish step self-skips.
        required: false

env:
  MOD_ID: ${{ inputs.mod-id || github.event.repository.name }}

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up JDK 21
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5.4.0
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build
        # The pushed tag is the single source of version truth: inject it as the
        # gradle mod_version so the built jar's version is exactly the tag. Mods
        # keep mod_version=0.0.0 in gradle.properties as the local/dev base,
        # which surfaces as 0.0.0+g<sha> off git when no tag is injected.
        run: ./gradlew build -Pmod_version="${GITHUB_REF_NAME#v}"

      - name: Derive release metadata
        id: meta
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          if [[ "$VERSION" == *-* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi
          echo "title=${MOD_ID^} v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Stage distributable jar
        # action-gh-release treats each files: line as an independent glob; a
        # leading ! is not a negation, so build/libs/<mod>-*.jar would also match
        # the sources and dev jars. Copy only the distributable jar into a clean
        # dir and upload from there instead.
        run: |
          mkdir -p dist
          shopt -s nullglob
          found=0
          for f in build/libs/${MOD_ID}-*.jar; do
            case "$f" in
              *-sources.jar|*-dev.jar) continue ;;
            esac
            cp "$f" dist/
            found=1
          done
          if [ "$found" -eq 0 ]; then
            echo "::error::No distributable jar found in build/libs for ${MOD_ID}"
            exit 1
          fi
          ls -l dist/

      - name: Gather changelog source
        # GitHub's own note generator is the factual ground truth — the real list
        # of merged PRs with the previous tag auto-detected. Claude rewrites it
        # below, and it is also the fallback body if the model is unavailable.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh api "repos/${GITHUB_REPOSITORY}/releases/generate-notes" \
              -f tag_name="${GITHUB_REF_NAME}" --jq '.body' \
              > /tmp/gh-notes.md 2>/tmp/gh-notes-err.txt; then
            echo "Gathered $(wc -l < /tmp/gh-notes.md) line(s) of source notes"
          else
            echo "::warning::Could not generate source notes"
            tail -c 500 /tmp/gh-notes-err.txt || true
            : > /tmp/gh-notes.md
          fi

      - name: Detect Claude auth
        id: auth
        # secrets context is not usable in a step `if:`, so map presence of the
        # token to a step output the changelog steps can gate on.
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        run: |
          if [ -n "$CLAUDE_CODE_OAUTH_TOKEN" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No CLAUDE_CODE_OAUTH_TOKEN; using GitHub's raw changelog"
          fi

      - name: Install Claude Code
        if: steps.auth.outputs.available == 'true'
        run: |
          # Fetch the installer to disk rather than piping the network stream
          # straight into bash: this captures the exact bytes (a truncated/MITM'd
          # response can't partially execute) and logs their digest for audit.
          curl -fsSL https://claude.ai/install.sh -o /tmp/claude-install.sh
          echo "claude install.sh sha256: $(sha256sum /tmp/claude-install.sh | cut -d' ' -f1)"
          # To pin against supply-chain tampering, set EXPECTED_SHA256 to the
          # logged digest once reviewed — install then fails closed on any change.
          EXPECTED_SHA256=""
          if [ -n "$EXPECTED_SHA256" ]; then
            echo "${EXPECTED_SHA256}  /tmp/claude-install.sh" | sha256sum -c - \
              || { echo "::error::claude install.sh checksum mismatch"; exit 1; }
          fi
          bash /tmp/claude-install.sh

      - name: Write release notes
        if: steps.auth.outputs.available == 'true'
        # Self-contained prompt: the merged-PR list is embedded, so the model
        # only rewrites what it is given and needs no tools. A failed or empty
        # run leaves /tmp/ai-notes.md absent and the next step uses the raw list.
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          MODEL: ${{ inputs.model }}
          MAX_TURNS: ${{ inputs.changelog-max-turns }}
          MAX_BUDGET: ${{ inputs.changelog-max-budget-usd }}
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          MOD_NAME="${MOD_ID^}"
          {
            cat <<EOF
          You are writing the public release notes for "${MOD_NAME}" v${VERSION}, a
          Vanilla+ Minecraft mod in the Concord collection.

          Rewrite the merged pull requests below into concise, friendly,
          player-facing release notes:
          - Open with one or two sentences on what this release is about.
          - Then group changes under "## Added", "## Changed", and "## Fixed".
            Omit any section that would be empty.
          - Write for players, not developers: no commit hashes, PR numbers,
            author handles, branch names, or internal/CI/build jargon.
          - Describe ONLY changes present in the list below — never invent, infer,
            or embellish features.
          - Keep it tight: a handful of bullets per section at most.
          Output only the Markdown release notes, with no preamble or sign-off.

          --- BEGIN MERGED PULL REQUESTS ---
          EOF
            cat /tmp/gh-notes.md
            cat <<'EOF'
          --- END MERGED PULL REQUESTS ---
          EOF
          } > /tmp/changelog-prompt.txt

          RESULT="$(~/.local/bin/claude -p \
            --output-format json \
            --model "$MODEL" \
            --max-turns "$MAX_TURNS" \
            --max-budget-usd "$MAX_BUDGET" \
            < /tmp/changelog-prompt.txt 2>/tmp/changelog-stderr.txt)" || true

          NOTES="$(printf '%s' "$RESULT" | jq -r '.result // empty' 2>/dev/null)" || true
          COST="$(printf '%s' "$RESULT" | jq -r '.total_cost_usd // 0' 2>/dev/null)" || COST=0
          TURNS="$(printf '%s' "$RESULT" | jq -r '.num_turns // 0' 2>/dev/null)" || TURNS=0

          if [ -n "$NOTES" ]; then
            printf '%s\n' "$NOTES" > /tmp/ai-notes.md
            echo "✅ Wrote release notes (\$${COST}, ${TURNS} turn(s), ${MODEL})"
            if [ -n "$GITHUB_STEP_SUMMARY" ]; then
              {
                echo "### 💲 Release notes"
                echo ""
                echo "| Model | Cost (USD) | Turns |"
                echo "|---|---|---|"
                echo "| ${MODEL} | \$${COST} | ${TURNS} |"
              } >> "$GITHUB_STEP_SUMMARY"
            fi
          else
            echo "::warning::Release-note model produced no output; falling back to raw notes"
            tail -c 1000 /tmp/changelog-stderr.txt || true
          fi

      - name: Assemble release body
        # Always produces /tmp/release-body.md so the release step has a body
        # regardless of whether the model ran. The published notes are the AI's
        # player-facing prose alone — no commit/PR list, contributor roll, or
        # compare links. The raw merged-PR list is used only as the fallback body
        # when the model was unavailable, never appended to the prose.
        env:
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          if [ -s /tmp/ai-notes.md ]; then
            cp /tmp/ai-notes.md /tmp/release-body.md
          elif [ -s /tmp/gh-notes.md ]; then
            cp /tmp/gh-notes.md /tmp/release-body.md
          else
            printf '%s v%s\n' "${MOD_ID^}" "$VERSION" > /tmp/release-body.md
          fi
          echo "--- release body ---"
          cat /tmp/release-body.md

      - name: Create draft GitHub Release
        # Created as a draft so the jar can be attached: repositories with
        # immutable releases enabled reject asset uploads after publish. The
        # Publish step below flips the draft to published once the jar is on it.
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          name: ${{ steps.meta.outputs.title }}
          tag_name: ${{ github.ref_name }}
          body_path: /tmp/release-body.md
          draft: true
          prerelease: ${{ steps.meta.outputs.prerelease == 'true' }}
          files: dist/${{ env.MOD_ID }}-*.jar

      - name: Publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release edit "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --draft=false

      - name: Detect publish auth
        id: publish
        # secrets context is not usable in a step `if:`, so map token presence to
        # a step output the platform-publish steps gate on. With neither token
        # both steps are skipped and the GitHub Release above is the only artifact.
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
          CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
        run: |
          if [ -n "$MODRINTH_TOKEN" ] || [ -n "$CURSEFORGE_TOKEN" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No MODRINTH_TOKEN/CURSEFORGE_TOKEN; skipping mod-platform publish"
          fi

      - name: Resolve release dependencies
        if: steps.publish.outputs.available == 'true'
        # Normalise fabric.mod.json's optional siblings (suggests + recommends)
        # into a slug list both publish steps consume. A repo-local
        # .github/release-slug-overrides.json remaps any mod id whose platform
        # slug differs from its Fabric id (e.g. roughlyenoughitems -> rei /
        # roughly-enough-items). A missing file, missing keys, or unparsable
        # fabric.mod.json all degrade to "no dependencies" rather than failing.
        run: |
          FMJ="$(find . -path '*/resources/fabric.mod.json' -print -quit 2>/dev/null || true)"
          if [ -z "$FMJ" ] || ! jq -e . "$FMJ" >/dev/null 2>&1; then
            echo "::notice::No parseable fabric.mod.json; no release dependencies"
            echo '[]' > /tmp/release-deps.json
            exit 0
          fi
          OVR='{}'
          if [ -f .github/release-slug-overrides.json ]; then
            OVR="$(cat .github/release-slug-overrides.json)"
          fi
          # Per dep: id from fabric.mod.json, platform slugs default to that id
          # but can be overridden per platform. suggests/recommends are optional.
          jq -n --argjson m "$(cat "$FMJ")" --argjson o "$OVR" '
            [ (($m.suggests // {}) | keys[]), (($m.recommends // {}) | keys[]) ]
            | unique
            | map(. as $id | {
                id: $id,
                modrinth: ($o[$id].modrinth // $id),
                curseforge: ($o[$id].curseforge // $id),
                type: "optional"
              })
          ' > /tmp/release-deps.json
          echo "Resolved $(jq 'length' /tmp/release-deps.json) optional dependency/ies:"
          jq -c '.[]' /tmp/release-deps.json || true

      - name: Publish to Modrinth
        if: steps.publish.outputs.available == 'true'
        # Direct POST /v2/version: Modrinth wants a multipart body with a `data`
        # JSON part plus the jar. The PAT goes in the Authorization header raw (no
        # "Bearer"). game_versions/loaders come from the pinned inputs, so the jar's
        # fabric.mod.json range can't widen the tag set. Self-skips with no token.
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
          MODRINTH_ID: ${{ inputs.modrinth-id || env.MOD_ID }}
          TITLE: ${{ steps.meta.outputs.title }}
          VERSION: ${{ steps.meta.outputs.version }}
          VERSION_TYPE: ${{ steps.meta.outputs.prerelease == 'true' && 'beta' || 'release' }}
          GAME_VERSIONS: ${{ inputs.game-versions }}
          LOADERS: ${{ inputs.loaders }}
          FEATURED: ${{ steps.meta.outputs.prerelease == 'true' && 'false' || 'true' }}
        run: |
          if [ -z "$MODRINTH_TOKEN" ]; then
            echo "::notice::No MODRINTH_TOKEN; skipping Modrinth publish"
            exit 0
          fi
          JAR="$(ls dist/${MOD_ID}-*.jar | head -1)"
          echo "Publishing $JAR to Modrinth project $MODRINTH_ID"

          # Modrinth wants each dependency as a project_id, so resolve every
          # slug to its id via the public project endpoint. Slugs that don't
          # resolve are warned and dropped — a stale sibling can't block release.
          DEPS_JSON='[]'
          if [ -s /tmp/release-deps.json ] && [ "$(jq 'length' /tmp/release-deps.json)" -gt 0 ]; then
            RESOLVED='[]'
            while IFS=$'\t' read -r slug dtype; do
              [ -z "$slug" ] && continue
              PID="$(curl -sS "https://api.modrinth.com/v2/project/${slug}" | jq -r '.id // empty')" || PID=""
              if [ -n "$PID" ]; then
                RESOLVED="$(printf '%s' "$RESOLVED" | jq --arg pid "$PID" --arg dt "$dtype" \
                  '. + [{project_id: $pid, dependency_type: $dt}]')"
              else
                echo "::warning::Modrinth dependency '$slug' did not resolve; skipping"
              fi
            done < <(jq -r '.[] | "\(.modrinth)\t\(.type)"' /tmp/release-deps.json)
            DEPS_JSON="$RESOLVED"
          fi
          echo "Modrinth dependencies: $(printf '%s' "$DEPS_JSON" | jq -c .)"

          # split() each csv input and trim whitespace into JSON string arrays.
          # The JSON goes to a file so curl reads it verbatim (`-F name=<file`) —
          # inline `-F name=$DATA` would let a stray ";type=" in the notes break
          # curl's field parsing.
          jq -n \
            --arg name "$TITLE" \
            --arg vn "$VERSION" \
            --rawfile cl /tmp/release-body.md \
            --arg vt "$VERSION_TYPE" \
            --arg pid "$MODRINTH_ID" \
            --arg gv "$GAME_VERSIONS" \
            --arg ld "$LOADERS" \
            --argjson ft "$FEATURED" \
            --argjson deps "$DEPS_JSON" \
            '{
              name: $name,
              version_number: $vn,
              changelog: $cl,
              dependencies: $deps,
              game_versions: ($gv | split(",") | map(gsub("^\\s+|\\s+$"; ""))),
              version_type: $vt,
              loaders: ($ld | split(",") | map(gsub("^\\s+|\\s+$"; "")) | map(ascii_downcase)),
              featured: $ft,
              project_id: $pid,
              file_parts: ["file"],
              primary_file: "file"
            }' > /tmp/modrinth-data.json

          attempt=0
          while :; do
            attempt=$((attempt + 1))
            HTTP="$(curl -sS -o /tmp/modrinth-resp.json -w '%{http_code}' \
              -X POST "https://api.modrinth.com/v2/version" \
              -H "Authorization: $MODRINTH_TOKEN" \
              -F "data=</tmp/modrinth-data.json;type=application/json" \
              -F "file=@${JAR};type=application/java-archive")" || HTTP=599
            if [ "$HTTP" -lt 300 ]; then break; fi
            if [ "$attempt" -ge 3 ]; then
              echo "::error::Modrinth upload failed (HTTP $HTTP)"
              cat /tmp/modrinth-resp.json || true
              exit 1
            fi
            echo "::warning::Modrinth HTTP $HTTP; retrying in 10s ($attempt/3)"
            sleep 10
          done
          echo "✅ Published to Modrinth ($(jq -r '.id // "?"' /tmp/modrinth-resp.json))"

      - name: Publish to CurseForge
        if: steps.publish.outputs.available == 'true'
        # Direct POST /api/projects/<id>/upload-file with a multipart metadata JSON
        # plus the jar, token in the X-Api-Token header. CurseForge wants numeric
        # version ids, so first GET /api/game/versions and resolve the pinned game
        # versions and loaders by name (loaders live in that same catalogue, e.g.
        # "Fabric"). changelogType=markdown so notes render. Self-skips with no
        # token or no curseforge-id.
        env:
          CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
          CURSEFORGE_ID: ${{ inputs.curseforge-id }}
          TITLE: ${{ steps.meta.outputs.title }}
          RELEASE_TYPE: ${{ steps.meta.outputs.prerelease == 'true' && 'beta' || 'release' }}
          GAME_VERSIONS: ${{ inputs.game-versions }}
          LOADERS: ${{ inputs.loaders }}
        run: |
          if [ -z "$CURSEFORGE_TOKEN" ] || [ -z "$CURSEFORGE_ID" ]; then
            echo "::notice::No CURSEFORGE_TOKEN or curseforge-id; skipping CurseForge publish"
            exit 0
          fi
          JAR="$(ls dist/${MOD_ID}-*.jar | head -1)"
          echo "Publishing $JAR to CurseForge project $CURSEFORGE_ID"

          # The catalogue is hundreds of KB, so write it to a file and let jq read
          # it as input — passing it via --argjson would blow ARG_MAX (E2BIG).
          curl -sS -H "X-Api-Token: $CURSEFORGE_TOKEN" \
            "https://minecraft.curseforge.com/api/game/versions" \
            -o /tmp/cf-versions.json || : > /tmp/cf-versions.json
          if ! jq -e 'type == "array" and length > 0' /tmp/cf-versions.json >/dev/null 2>&1; then
            echo "::error::Could not fetch the CurseForge game-versions catalogue"
            exit 1
          fi

          # Match the pinned game versions + loaders against the catalogue by name
          # (case-insensitive) and collect their numeric ids.
          WANTED="${GAME_VERSIONS},${LOADERS}"
          GV_IDS="$(jq --arg wanted "$WANTED" '
            ($wanted | split(",") | map(ascii_downcase | gsub("^\\s+|\\s+$"; ""))) as $w
            | [ .[] | select(((.name | ascii_downcase) as $n | $w | index($n)) != null) | .id ]' \
            /tmp/cf-versions.json)"
          COUNT="$(printf '%s' "$GV_IDS" | jq 'length')"
          if [ "$COUNT" -eq 0 ]; then
            echo "::error::Could not resolve any CurseForge version ids for: $WANTED"
            exit 1
          fi
          echo "Resolved CurseForge version ids: $(printf '%s' "$GV_IDS" | jq -c .)"

          # CurseForge takes dependencies by slug directly, under relations.projects.
          CF_PROJECTS='[]'
          if [ -s /tmp/release-deps.json ]; then
            CF_PROJECTS="$(jq '[.[] | {
              slug: .curseforge,
              type: (if .type == "required" then "requiredDependency" else "optionalDependency" end)
            }]' /tmp/release-deps.json)"
          fi
          echo "CurseForge dependencies: $(printf '%s' "$CF_PROJECTS" | jq -c '[.[].slug]')"

          # relations is omitted when empty (CurseForge rejects an empty projects list).
          write_meta() {  # $1: 1 -> include relations, 0 -> omit them
            jq -n \
              --rawfile cl /tmp/release-body.md \
              --arg dn "$TITLE" \
              --arg rt "$RELEASE_TYPE" \
              --argjson gv "$GV_IDS" \
              --argjson rel "$CF_PROJECTS" \
              --argjson inc "$1" \
              '{changelog: $cl, changelogType: "markdown", displayName: $dn, gameVersions: $gv, releaseType: $rt}
               + (if $inc == 1 and ($rel | length) > 0 then {relations: {projects: $rel}} else {} end)' \
              > /tmp/cf-meta.json
          }
          INCLUDE_REL=1
          [ "$(printf '%s' "$CF_PROJECTS" | jq 'length')" -gt 0 ] || INCLUDE_REL=0
          write_meta "$INCLUDE_REL"

          attempt=0
          while :; do
            attempt=$((attempt + 1))
            HTTP="$(curl -sS -o /tmp/cf-resp.json -w '%{http_code}' \
              -X POST "https://minecraft.curseforge.com/api/projects/${CURSEFORGE_ID}/upload-file" \
              -H "X-Api-Token: $CURSEFORGE_TOKEN" \
              -F "metadata=</tmp/cf-meta.json;type=application/json" \
              -F "file=@${JAR};type=application/java-archive")" || HTTP=599
            if [ "$HTTP" -lt 300 ]; then break; fi
            if [ "$attempt" -ge 3 ]; then
              # Dependencies are best-effort: a bad sibling slug must not block the
              # release, so drop relations and give the upload one more clean run.
              if [ "$INCLUDE_REL" -eq 1 ]; then
                echo "::warning::CurseForge upload failed (HTTP $HTTP); retrying once without optional dependencies"
                cat /tmp/cf-resp.json || true
                INCLUDE_REL=0
                write_meta 0
                attempt=0
                continue
              fi
              echo "::error::CurseForge upload failed (HTTP $HTTP)"
              cat /tmp/cf-resp.json || true
              exit 1
            fi
            echo "::warning::CurseForge HTTP $HTTP; retrying in 10s ($attempt/3)"
            sleep 10
          done
          echo "✅ Published to CurseForge ($(jq -r '.id // "?"' /tmp/cf-resp.json))"
